apiVersion: v1
kind: Pod
metadata:
  name: prizegen
  labels: 
    app: prizegen
spec:
  containers:
    - name: prizegen
      image: hazardd/prizegen:big
      imagePullPolicy: "Always"
      ports:
      - containerPort: 9017
